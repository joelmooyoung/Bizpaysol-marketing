[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist/spa"
  # Force rebuild with updated GEMINI_API_KEY env var (2025-12-03)

[security.headers.Content-Security-Policy]
  enable = true

[security.scan]
  skip_npm_audit = false

[[plugins]]
  package = "netlify-plugin-cache-nextjs"

[functions]
  external_node_modules = ["express"]
  node_bundler = "esbuild"
  
[[redirects]]
  force = true
  from = "/api/*"
  status = 200
  to = "/.netlify/functions/api/:splat"

# SPA fallback for client-side routing (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = "default-src 'self'; base-uri 'self' *; frame-ancestors 'none'; form-action 'self'; script-src 'self' 'unsafe-inline' https://cdn.botpenguin.com https://window-2.botpenguin.com; style-src 'self' 'unsafe-inline' https://cdn.botpenguin.com; img-src 'self' data: blob: https:; font-src 'self' data: https:; connect-src 'self' https: wss:; frame-src 'self' https://window-2.botpenguin.com https://window.botpenguin.com https://cdn.botpenguin.com; object-src 'none'"
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "accelerometer=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=()"
    Cross-Origin-Opener-Policy = "same-origin"
    Cross-Origin-Resource-Policy = "same-origin"
    Cross-Origin-Embedder-Policy = "credentialless"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"
